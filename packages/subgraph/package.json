{
  "name": "@web3-university/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-contracts.mjs",
    "codegen": "pnpm sync && graph codegen",
    "build": "pnpm codegen && graph build --network sepolia",
    "typecheck": "pnpm build",
    "test": "pnpm build",
    "test:mapping": "graph test",
    "deploy:studio": "node scripts/deploy-studio.mjs"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
