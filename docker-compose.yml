services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: web3_university
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - web3_university_pg:/var/lib/postgresql/data

volumes:
  web3_university_pg:
